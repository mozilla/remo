<div id="unavailable-profile-msg">
  You 're currently unavailable {%- if date %} until {{ date }} {% endif -%}.
  Want to  <a href="{{ url('edit_availability', display_name) }}">
    become available again?
  </a>
</div>
